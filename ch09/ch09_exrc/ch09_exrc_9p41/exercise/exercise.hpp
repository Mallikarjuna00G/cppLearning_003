/**
 * @file exercise.hpp
 */

#ifndef EXERCISE_HPP
#define EXERCISE_HPP

/**
 * @brief Initialize a `string` from a `vector<char>`.
 * 
 * @details Exercise 9.41: Write a program that initializes a `string` from a `vector<char>`.
 *
 * @return `EXIT_SUCCESS` on successful completion.
 */
int exercise();

#endif // EXERCISE_HPP