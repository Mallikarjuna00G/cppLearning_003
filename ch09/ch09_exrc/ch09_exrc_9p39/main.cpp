/**
 * @file main.cpp
 * @brief Main entry point of the application.
 *
 * This file contains the `main` function, which serves as the program's
 * primary entry point. Its sole purpose is to call the `exercise()` function.
 */
#include "exercise.hpp"

/**
 * @brief The main function.
 *
 * This function initiates the program by calling `exercise()`.
 *
 * @return The exit code returned by `exercise()`.
 */
int main() {
    return exercise();
}