#ifndef REC_VEC_H
#define REC_VEC_H

#include <iostream>
#include <vector>
#include <cstddef>

void printVec(std::vector<int> &vec);
void recVec(std::vector<int> &vec, std::vector<int>::size_type index);

#endif